package main

import "fmt"

func main() {
	fmt.Println("Building exe for different os")

	// in terminal set GOOS to either windows, darwin or linux
	// and then run command go build
	// run go build -o <name> to specify exe file name
}
